# coding: utf-8
from smart_kit.handlers.handler_base import HandlerBase


class CustomHandler(HandlerBase):

    """
        Тут создаются Handlers, которые используются для запуска логики в зависимости от типа входяшего сообщения
    """

    def run(self, payload, user):
        return []
